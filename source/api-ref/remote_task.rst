****************************
actuator.remote_task module
****************************

**Import package**: actuator.remote_task

.. automodule:: actuator.remote_task
    :members:

=========
Functions
=========

.. autofunction:: actuator.remote_task.with_dependencies

.. note:: the :py:func:`with_remote_options` function is aliased in the
    config (with_config_options())
    and execute (with_execute_options()) modules, but they are all the same function.

.. autofunction:: actuator.remote_task.with_remote_options
